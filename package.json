{
  "name": "project-new-clean",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/common/*"
  ],
  "scripts": {
    "build": "yarn workspace backend build && yarn workspace admin-panel build",
    "start:dev": "concurrently \"yarn workspace backend start:dev\" \"yarn workspace admin-panel dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
